## 1. Migración y esquema (subagente: backend-architect) — COMPLETADO (commit 246344f)

- [x] 1.1 Crear `V4__checkup_schema.sql` con tablas `checkups`, `checkup_times`, `national_reference_times` (NOTA: no se crea tabla `medal_projections` por D5 del design — `MedalProjection` es modelo JVM derivado en tiempo de consulta), con campos `created_at`/`updated_at`/`deleted_at` (`TIMESTAMP WITH TIME ZONE`) y constraints CHECK de soft delete siguiendo el patrón de V3
- [x] 1.2 Definir columnas de `checkups`: `id BIGSERIAL PK`, `athlete_id BIGINT NOT NULL` (FK `athletes.id ON DELETE RESTRICT`), `year INTEGER NOT NULL`, `month INTEGER NOT NULL CHECK (month BETWEEN 1 AND 12)`, `category VARCHAR(30) NOT NULL`, `notes TEXT`, `created_at`, `updated_at`, `deleted_at`; constraint `UNIQUE (athlete_id, year, month, category) WHERE deleted_at IS NULL` y `CHECK (year BETWEEN 1900 AND 2100)`
- [x] 1.3 Definir columnas de `checkup_times`: `id BIGSERIAL PK`, `checkup_id BIGINT NOT NULL` (FK `checkups.id ON DELETE RESTRICT` en DB por D8), `style VARCHAR(20) NOT NULL`, `distance INTEGER NOT NULL CHECK (distance > 0)`, `time_seconds NUMERIC(10,3) NOT NULL CHECK (time_seconds > 0)`, `created_at`, `updated_at`, `deleted_at`; constraint `UNIQUE (checkup_id, style, distance) WHERE deleted_at IS NULL` y `INDEX (checkup_id) WHERE deleted_at IS NULL`
- [x] 1.4 Definir columnas de `national_reference_times`: `id BIGSERIAL PK`, `style VARCHAR(20) NOT NULL`, `distance INTEGER NOT NULL CHECK (distance > 0)`, `category VARCHAR(30) NOT NULL`, `position SMALLINT NOT NULL CHECK (position IN (1,2,3))`, `time_seconds NUMERIC(10,3) NOT NULL CHECK (time_seconds > 0)`, `created_at`, `updated_at`, `deleted_at`; constraint `UNIQUE (style, distance, category, position) WHERE deleted_at IS NULL` e `INDEX (style, distance, category) WHERE deleted_at IS NULL`
- [x] 1.5 Agregar índices parciales `WHERE deleted_at IS NULL` para todas las consultas frecuentes (listado de checkups por atleta, listado de tiempos por checkup, lookup de tabla nacional por style+distance+category) y `COMMENT ON` de tablas/columnas siguiendo el patrón V2/V3
- [x] 1.6 Documentar script de rollback reversible al final del archivo (`DROP TABLE checkup_times; DROP TABLE national_reference_times; DROP TABLE checkups;` — respetar FK order) y verificar `./mvnw compile` (BUILD SUCCESS; Flyway se aplica al arranque del app, validado en Fase D)

## 2. Entidades, enums y repositorios (subagente: checkup-domain) — COMPLETADO (sin commitear, working tree — revisión pendiente del usuario antes de Fase C)

- [x] 2.1 Crear enum `CheckupCategory` (valores iniciales `INFANTIL`, `JUVENIL`, `MAYOR`) y enum `SwimmingStyle` (LIBRE, ESPALDA, BRAZA, MARIPOSA, COMBINADO) — ubicados en `checkup/` raíz
- [x] 2.2 Crear entidad `Checkup extends BaseEntity` en `checkup/Checkup.java` con `@SQLDelete`/`@SQLRestriction`, `@SequenceGenerator` (`checkups_id_seq`), `@ManyToOne(fetch = LAZY)` a `Athlete`, `category` String (validado por enum en DTO, no en entidad)
- [x] 2.3 Crear entidad `CheckupTime extends BaseEntity` en `checkup/CheckupTime.java` con `@SQLDelete`/`@SQLRestriction`, `@ManyToOne` a `Checkup`, `style` String, `distance` Integer, `timeSeconds` BigDecimal (`precision=10, scale=3`)
- [x] 2.4 Crear entidad `NationalReferenceTime extends BaseEntity` en `checkup/NationalReferenceTime.java` con `@SQLDelete`/`@SQLRestriction`, `position` Short (`@Min(1) @Max(3)`), `timeSeconds` BigDecimal, `style`/`distance`/`category`
- [x] 2.5 Crear `MedalProjection` como **record** en `checkup/MedalProjection.java` (no `dto/` — es POJO de dominio no DTO; el wrapper HTTP `MedalProjectionResponse` va en `dto/`); NO `@Entity` (D5); campos: `style`, `distance`, `category`, `classification`, `timeSeconds`, `diffVsBronzeSeconds`
- [x] 2.6 Crear repositorios `CheckupRepository`, `CheckupTimeRepository`, `NationalReferenceTimeRepository` con consultas: `findActiveByAthleteId`, `findActiveByAthleteIdAndYearMonth`, `findActiveByCheckupId`, `findActiveTripleByStyleDistanceCategory` (una query, ordenado por position ASC), `existsActive...` para los 3 UNIQUE de V4 (necesarios para 409 en Fase C). Se usaron `@Query` JPQL explícito donde Spring Data no deriva `findActive...`

## 3. DTOs y formatter (subagente: checkup-domain) — COMPLETADO (sin commitear)

- [x] 3.1 Crear `TimeFormatter` (utilidad estática en `checkup/TimeFormatter.java`) que convierte `BigDecimal` segundos → `mm:ss.ms` (`hh:mm:ss.ms` para >=3600s) y viceversa. Negativos → `IllegalArgumentException`; `null` → `null` (presentación tolerante). Incluye `toSignedFormatted` para deltas negativos. La entidad NUNCA formatea; solo DTOs.
- [x] 3.2 Crear DTOs request/response para `Checkup` (records) en `checkup/dto/`: `CreateCheckupRequest`, `UpdateCheckupRequest`, `CheckupResponse` (con `fromCheckup(Checkup)`), `CheckupDetailResponse` (con `fromCheckup(Checkup, List<CheckupTime>)`), `AddCheckupTimeRequest`, `CheckupTimeResponse` (incluye `timeSeconds` + `timeFormatted`)
- [x] 3.3 Crear DTOs para tabla nacional: `NationalReferenceTimeRequest`, `NationalReferenceTimeResponse`, `NationalReferenceTimeListItemResponse`
- [x] 3.4 Crear DTOs de comparación y proyección: `TimeComparisonResponse` (con record anidado `PositionComparison` — delta absoluto + relativo %, factory `fromCheckupTimeAndReferences`), `MedalProjectionResponse` (wrapper HTTP con `fromMedalProjection(MedalProjection)`), `Classification` como enum en `checkup/Classification.java` (`POR_ENCIMA_DEL_PODIO`, `CERCANO_A_MEDALLERIA`, `FUERA_DE_RANGO`)

> Notas del subagente para Fase C: (a) `DuplicateResourceException` (HTTP 409) ya existe en `common/exception` — usarla en lugar de crear `ConflictException`. (b) DTOs request con `@Pattern` reflexionando valores del enum para `category`/`style` (mantenible al crecer enums).

## 4. Servicios (subagente: checkup-domain) — COMPLETADO (commit 06095f8)

- [x] 4.1 Crear `CheckupService`: registro de chequeo (validación de atleta existente y único-activo `(athlete_id, year, month, category)`), agregar `CheckupTime` con unicidad `(style, distance)`, lectura por ID y por atleta, soft delete; lanza `ResourceNotFoundException` (404) y `DuplicateResourceException` (409, ya existente en `common/exception`)
- [x] 4.2 Crear `NationalReferenceTimeService`: CRUD admin de la tabla nacional, validación de `position IN (1,2,3)` (defensiva → `ValidationException` 400) y unicidad `(style, distance, category, position)` solo activos; lectura pública (sin `@PreAuthorize` en métodos READ)
- [x] 4.3 Crear `TimeComparisonService`: dado un `CheckupTime` (style, distance, category), carga el triple activo de `national_reference_times` (1°/2°/3°), lanza `DuplicateResourceException` (HTTP 409) si falta el triple completo; calcula diferencia absoluta (`trialSeconds - refSeconds`) y relativa para cada posición (delegado en la factory `TimeComparisonResponse.fromCheckupTimeAndReferences`); sin `Clock` (la comparación no toca fechas)
- [x] 4.4 Crear `ClassificationService`: lee `checkup.medal-proximity-threshold-seconds` vía `@Value` en constructor (default `1.500`, override por env var `CHECKUP_MEDAL_PROXIMITY_THRESHOLD_SECONDS`); dado `trialSeconds` vs `bronzeSeconds`, retorna `POR_ENCIMA_DEL_PODIO` si `trialSeconds <= bronzeSeconds`, `CERCANO_A_MEDALLERIA` si `0 < diff <= threshold`, `FUERA_DE_RANGO` en otro caso — lógica pura con `compareTo` sobre `BigDecimal` (determinística, testeable con datos fijos)
- [x] 4.5 Crear `MedalProjectionService`: por `athleteId`, recorre `CheckupTime` activos y para cada uno llama a `TimeComparisonService.loadReferenceTriple` + `ClassificationService.classify`, construye lista de `MedalProjection` (record); NO persiste (`@Transactional(readOnly = true)`); cache por query de triples minimiza round-trips (cubre budget 500 ms / 50 atletas); sin `Clock` (no toca fechas)
- [x] 4.6 Exponer propiedad `checkup.medal-proximity-threshold-seconds=${CHECKUP_MEDAL_PROXIMITY_THRESHOLD_SECONDS:1.500}` en `application.properties` (env var placeholder consistente con `training.attendance.absence-threshold`); también en `application-local.properties` (gitignored); `@Value` permite override por env var sin recodificar

## 5. Controllers y seguridad (subagente: checkup-domain) — COMPLETADO (commit 06095f8)

- [x] 5.1 Crear `CheckupController` en `/api/v1` (con paths explícitos por método, 1 controller para 2 raíces `/checkups` y `/athletes/{athleteId}/checkups`): `POST` (crear chequeo), `GET` (listar con filtros month/year), `GET /{id}` (detalle con tiempos), `DELETE /{id}` (soft delete), `POST /{checkupId}/times` (agregar tiempo); `@PreAuthorize("hasAnyRole('ADMIN','COACH')")` en escrituras (siguiendo el patrón de `TrainingPlanController` de training), READ autenticado sin `@PreAuthorize`
- [x] 5.2 Métodos de comparación y proyección integrados en `CheckupController`: `GET /api/v1/checkups/{checkupId}/comparison` (lista de `TimeComparisonResponse`), `GET /api/v1/athletes/{athleteId}/projections` (lista de `MedalProjectionResponse`)
- [x] 5.3 Crear `NationalReferenceTimeController` en `/api/v1/national-reference-times`: `POST`/`PUT`/`DELETE` con `@PreAuthorize("hasRole('ADMIN')")`, `GET` y `GET /{id}` autenticados (sin `@PreAuthorize`)
- [x] 5.4 `SecurityConfig` NO modificada — `anyRequest().authenticated()` (línea 52 existente) cubre los endpoints del checkup; `/api/v1/athletes/*/attendance` del módulo training no colisiona con `/api/v1/athletes/{athleteId}/checkups` (segmentos literales distintos)
- [x] 5.5 `GlobalExceptionHandler` NO modificada — los handlers existentes cubren: `ResourceNotFoundException` → 404, `DuplicateResourceException` → 409, `MethodArgumentNotValidException` → 400, `AccessDeniedException` (Spring Security) → 403; sin `catch (Exception e)` genérico en controllers

## 6. Tests y auditoría final (subagentes: checkup-domain + quality-guardian) — COMPLETADO (commits 73c684c D-1, 96f5228 fix-up, ed170d4 D-3)

- [x] 6.1 `CheckupServiceTest` (JUnit5 + Mockito, `@DisplayName` español): crear chequeo OK, dup `(athlete, year, month, category)` → 409, atleta inexistente → 404, agregar tiempo OK, dup `(style, distance)` → 409, soft delete exitoso, listado excluye soft-deleted
- [x] 6.2 `NationalReferenceTimeServiceTest`: CRUD admin OK, `position=4` → 400, dup `(style, distance, category, position)` → 409, lectura pública sin rol admin
- [x] 6.3 `TimeComparisonServiceTest` con datos fijos (ver spec `time-comparison`): trial < gold (diferencias negativas), trial entre posiciones, trial > bronze, falt triple incompleto → 409, determinismo entre runs con mismo input
- [x] 6.4 `ClassificationServiceTest` con threshold configurable (mock `@Value`): dentro de podio (trial < bronze) → `POR_ENCIMA_DEL_PODIO`, dentro de threshold → `Cercano`, fuera -> `FUERA_DE_RANGO`; verificar que cambio de threshold cambia la clasificación sin recodificar
- [x] 6.5 `MedalProjectionServiceTest`: proyección por atleta con 3 tiempos → 3 entries, atleta sin checkups → lista vacía (no error), verificación que no haya persistencia (mock `CheckupTimeRepository` y verificar que `save` nunca se llama)
- [x] 6.6 `TimeFormatterTest`: 65.250 → `01:05.250`, 125.755 → `02:05.755`, negativo → error/null, round-trip segundos→formato→segundos
- [x] 6.7 `./mvnw compile` pasa sin warnings nuevos; `./mvnw test` con tests focused del módulo → 72/72 verdes (59 unitarios en commit 73c684c + 13 e2e en commit ed170d4). Tests `@SpringBootTest` requieren PostgreSQL vivo (`athletecore-postgres`); tests de servicios son unitarios sin context Spring.
- [x] 6.8 Auditoría final con `quality-guardian`: validó `BaseEntity` extension, `@SQLRestriction` (no `@Where`) en las 3 entidades, DTOs sin entidades JPA en responses, `@PreAuthorize` en escrituras admin, `Athlete.java` NO modificado, `SecurityConfig` sin `permitAll()` nuevos. Reportó 4 hallazgos Baja (3 dead code + 1 `Instant.now()` directo en soft delete) y 2 escenarios spec sin test automatizado (cubiertos en commit ed170d4 con tests e2e `@WebMvcTest` + `@DataJpaTest`). Fix-up dead code en commit 96f5228; hallazgo `Instant.now()` queda como deuda menor registrada en SESSION_NOTES.
- [x] 6.9 Actualizar `TASKS.md` raíz: módulo Checkup marcado ✅ COMPLETADO; fila `medal_projections` ajustada como deuda D5 explícita con bloques descriptivos en TASKS.md raíz (estado de la tabla y módulo); `v0.4.0` marcado ✅; fecha actualizada a 2026-08-13. NO se creó CHANGELOG.md (política de repo). Cierre confirmado al usuario antes de finalizar la sesión.
