## 1. Migración y esquema (subagente: backend-architect)

- [ ] 1.1 Crear `V4__checkup_schema.sql` con tablas `checkups`, `checkup_times`, `national_reference_times` (NOTA: no se crea tabla `medal_projections` por D5 del design — `MedalProjection` es modelo JVM derivado en tiempo de consulta), con campos `created_at`/`updated_at`/`deleted_at` (`TIMESTAMP WITH TIME ZONE`) y constraints CHECK de soft delete siguiendo el patrón de V3
- [ ] 1.2 Definir columnas de `checkups`: `id BIGSERIAL PK`, `athlete_id BIGINT NOT NULL` (FK `athletes.id ON DELETE RESTRICT`), `year INTEGER NOT NULL`, `month INTEGER NOT NULL CHECK (month BETWEEN 1 AND 12)`, `category VARCHAR(30) NOT NULL`, `notes TEXT`, `created_at`, `updated_at`, `deleted_at`; constraint `UNIQUE (athlete_id, year, month, category) WHERE deleted_at IS NULL` y `CHECK (year BETWEEN 1900 AND 2100)`
- [ ] 1.3 Definir columnas de `checkup_times`: `id BIGSERIAL PK`, `checkup_id BIGINT NOT NULL` (FK `checkups.id ON DELETE CASCADE` lógico — RESTRICT en DB por consistencia con D8), `style VARCHAR(20) NOT NULL`, `distance INTEGER NOT NULL CHECK (distance > 0)`, `time_seconds NUMERIC(10,3) NOT NULL CHECK (time_seconds > 0)`, `created_at`, `updated_at`, `deleted_at`; constraint `UNIQUE (checkup_id, style, distance) WHERE deleted_at IS NULL` y `INDEX (checkup_id) WHERE deleted_at IS NULL`
- [ ] 1.4 Definir columnas de `national_reference_times`: `id BIGSERIAL PK`, `style VARCHAR(20) NOT NULL`, `distance INTEGER NOT NULL CHECK (distance > 0)`, `category VARCHAR(30) NOT NULL`, `position SMALLINT NOT NULL CHECK (position IN (1,2,3))`, `time_seconds NUMERIC(10,3) NOT NULL CHECK (time_seconds > 0)`, `created_at`, `updated_at`, `deleted_at`; constraint `UNIQUE (style, distance, category, position) WHERE deleted_at IS NULL` e `INDEX (style, distance, category) WHERE deleted_at IS NULL`
- [ ] 1.5 Agregar índices parciales `WHERE deleted_at IS NULL` para todas las consultas frecuentes (listado de checkups por atleta, listado de tiempos por checkup, lookup de tabla nacional por style+distance+category) y `COMMENT ON` de tablas/columnas siguiendo el patrón V2/V3
- [ ] 1.6 Documentar script de rollback reversible al final del archivo (`DROP TABLE checkup_times; DROP TABLE national_reference_times; DROP TABLE checkups;` — respetar FK order) y verificar que `./mvnw -DskipTests package` levanta Flyway (`spring.jpa.hibernate.ddl-auto=validate` no debe fallar)

## 2. Entidades, enums y repositorios (subagente: checkup-domain)

- [ ] 2.1 Crear enum `CheckupCategory` (valores iniciales `INFANTIL`, `JUVENIL`, `MAYOR` — extensible; validar con el usuario en fase A) y enum `SwimmingStyle` (LIBRE, ESPALDA, BRAZA, MARIPOSA, COMBINADO) según deportes soportados por `Sport` existente
- [ ] 2.2 Crear entidad `Checkup extends BaseEntity` en `checkup/Checkup.java` con `@SQLDelete(sql = "UPDATE checkups SET deleted_at = now(), updated_at = now() WHERE id=?")`, `@SQLRestriction("deleted_at IS NULL")`, `@SequenceGenerator` (siguiendo el patrón de `Athlete.java:52`), relación `@ManyToOne(fetch = LAZY)` a `Athlete`, `category` como `String` validado por el enum en la entrada (no restricción DB)
- [ ] 2.3 Crear entidad `CheckupTime extends BaseEntity` en `checkup/CheckupTime.java` con `@SQLDelete`/`@SQLRestriction`, `@ManyToOne` a `Checkup`, `style` (String validado por enum), `distance` (Integer), `timeSeconds` (`BigDecimal`) — tipo JPA `NUMERIC(10,3)`
- [ ] 2.4 Crear entidad `NationalReferenceTime extends BaseEntity` en `checkup/NationalReferenceTime.java` con `@SQLDelete`/`@SQLRestriction`, `position` (Short con `@Min(1) @Max(3)`), `timeSeconds` (`BigDecimal`), `style`, `distance`, `category`
- [ ] 2.5 Crear `MedalProjection` como **record** en `checkup/dto/MedalProjectionResponse.java` o `checkup/MedalProjection.java` (POJO de dominio NO `@Entity` — ver D5); campos: `style`, `distance`, `category`, `classification`, `timeSeconds`, `diffVsBronzeSeconds`
- [ ] 2.6 Crear repositorios `CheckupRepository`, `CheckupTimeRepository`, `NationalReferenceTimeRepository` (consultas: `findActiveByAthleteId`, `findActiveByAthleteIdAndYearMonth`, `findActiveByCheckupId`, `findActiveTripleByStyleDistanceCategory` que retorne las 3 posiciones ordenadas)

## 3. DTOs y formatter (subagente: checkup-domain)

- [ ] 3.1 Crear `TimeFormatter` (utilidad estática o componente Spring) que convierta `BigDecimal` segundos → `mm:ss.ms` y viceversa, con validación de input negativo/nulo (devolver `null` o lanzar `IllegalArgumentException` según convención) — la entidad NUNCA formatea; solo DTOs
- [ ] 3.2 Crear DTOs request/response para `Checkup` (records) en `checkup/dto/`: `CreateCheckupRequest`, `UpdateCheckupRequest`, `CheckupResponse`, `CheckupDetailResponse` (con tiempos embebidos), `AddCheckupTimeRequest`, `CheckupTimeResponse`
- [ ] 3.3 Crear DTOs para tabla nacional: `NationalReferenceTimeRequest`, `NationalReferenceTimeResponse`, `NationalReferenceTimeListItemResponse`
- [ ] 3.4 Crear DTOs de comparación y proyección: `TimeComparisonResponse` (por posición: 1°/2°/3° con diferencia absoluta y relativa en segundos y `mm:ss.ms`), `MedalProjectionResponse` (ver 2.5), `Classification` como enum (`POR_ENCIMA_DEL_PODIO`, `CERCANO_A_MEDALLERIA`, `FUERA_DE_RANGO`)

## 4. Servicios (subagente: checkup-domain)

- [ ] 4.1 Crear `CheckupService`: registro de chequeo (validación de atleta existente y único-activo `(athlete_id, year, month, category)`), agregar `CheckupTime` con unicidad `(style, distance)`, lectura por ID y por atleta, soft delete; lanza `ResourceNotFoundException` y `ConflictException` (o equivalente en `common/exception`)
- [ ] 4.2 Crear `NationalReferenceTimeService`: CRUD admin de la tabla nacional, validación de `position IN (1,2,3)` y unicidad `(style, distance, category, position)` solo activos; lectura pública (sin `@PreAuthorize` en métodos READ)
- [ ] 4.3 Crear `TimeComparisonService`: dado un `CheckupTime` (style, distance, category), carga el triple activo de `national_reference_times` (1°/2°/3°), lanza `ConflictException` si falta el triple completo; calcula diferencia absoluta (`trialSeconds - refSeconds`) y relativa para cada posición; usa `Clock` inyectado solo si tiene lógica日期 (preferible sin `Clock` si la comparación no toca fechas)
- [ ] 4.4 Crear `ClassificationService`: lee `checkup.medal-proximity-threshold-seconds` vía `@Value` (default `1.500`); dado `trialSeconds` vs `bronzeSeconds`, retorna `POR_ENCIMA_DEL_PODIO` si `trialSeconds <= bronzeSeconds`, `CERCANO_A_MEDALLERIA` si `0 < (trialSeconds - bronzeSeconds) <= threshold`, `FUERA_DE_RANGO` en otro caso — lógica pura, determinística, testeable con datos fijos
- [ ] 4.5 Crear `MedalProjectionService`: por `athleteId`, recorre `CheckupTime` activos y para cada uno llama a `TimeComparisonService` + `ClassificationService`, construye lista de `MedalProjection` (record); NO persiste (`@Transactional(readOnly = true)`); `Clock` solo si lógica de filtrado fecha
- [ ] 4.6 Exponer propiedad `checkup.medal-proximity-threshold-seconds=1.500` en `application.properties` y `application-local.properties`; validar que `@Value` permite override por env var

## 5. Controllers y seguridad (subagente: checkup-domain)

- [ ] 5.1 Crear `CheckupController` en `/api/v1/checkups` y `/api/v1/athletes/{athleteId}/checkups`: `POST` (crear chequeo), `GET` (listar con filtros month/year), `GET /{id}` (detalle con tiempos), `DELETE /{id}` (soft delete), `POST /{checkupId}/times` (agregar tiempo); `@PreAuthorize("hasRole('ADMIN')")` en escrituras, READ autenticado sin `@PreAuthorize`
- [ ] 5.2 Crear `CheckupComparisonController` (o metódos en `CheckupController`): `GET /api/v1/checkups/{checkupId}/comparison` (comparación de todos los tiempos del chequeo vs tabla nacional), `GET /api/v1/athletes/{athleteId}/projections` (proyección de medallería — lista de `MedalProjectionResponse`)
- [ ] 5.3 Crear `NationalReferenceTimeController` en `/api/v1/national-reference-times`: `POST`/`PUT`/`DELETE` con `@PreAuthorize("hasRole('ADMIN')")`, `GET` autenticado
- [ ] 5.4 Verificar que `SecurityConfig` no añade `permitAll()` para estos endpoints (regla por defecto autenticado cubre); confirmar que los endpoints `/api/v1/athletes/{athleteId}/checkups` son distintos de los de `/api/v1/athletes/{id}/sports` del módulo athlete (sin path collision)
- [ ] 5.5 Manejo de errores centralizado en `GlobalExceptionHandler`: mapear `ResourceNotFoundException` → 404, `ConflictException` → 409, validaciones Jakarta → 400, `AccessDeniedException` → 403; no `catch (Exception e)` genérico en controllers

## 6. Tests y auditoría final (subagentes: checkup-domain + quality-guardian)

- [ ] 6.1 `CheckupServiceTest` (JUnit5 + Mockito, `@DisplayName` español): crear chequeo OK, dup `(athlete, year, month, category)` → 409, atleta inexistente → 404, agregar tiempo OK, dup `(style, distance)` → 409, soft delete exitoso, listado excluye soft-deleted
- [ ] 6.2 `NationalReferenceTimeServiceTest`: CRUD admin OK, `position=4` → 400, dup `(style, distance, category, position)` → 409, lectura pública sin rol admin
- [ ] 6.3 `TimeComparisonServiceTest` con datos fijos (ver spec `time-comparison`): trial < gold (diferencias negativas), trial entre posiciones, trial > bronze, falt triple incompleto → 409, determinismo entre runs con mismo input
- [ ] 6.4 `ClassificationServiceTest` con threshold configurable (mock `@Value`): dentro de podio (trial < bronze) → `POR_ENCIMA_DEL_PODIO`, dentro de threshold → `Cercano`, fuera -> `FUERA_DE_RANGO`; verificar que cambio de threshold cambia la clasificación sin recodificar
- [ ] 6.5 `MedalProjectionServiceTest`: proyección por atleta con 3 tiempos → 3 entries, atleta sin checkups → lista vacía (no error), verificación que no haya persistencia (mock `CheckupTimeRepository` y verificar que `save` nunca se llama)
- [ ] 6.6 `TimeFormatterTest`: 65.250 → `01:05.250`, 125.755 → `02:05.755`, negativo → error/null, round-trip segundos→formato→segundos
- [ ] 6.7 Verificar `./mvnw compile` pasa sin warnings nuevos; ejecutar module tests focused: `./mvnw test -Dtest='*Checkup*Test,*TimeComparison*Test,*Classification*Test,*MedalProjection*Test,*NationalReference*Test'`. Nota: tests `@SpringBootTest` requieren PostgreSQL vivo (igual que módulos previos); tests de servicios son unitarios sin context Spring.
- [ ] 6.8 Auditoría final con `quality-guardian`: valida `BaseEntity` extension, `@SQLRestriction` (no `@Where`) en las 3 entidades, DTOs sin entidades JPA en responses, `@PreAuthorize` en escrituras admin, `Athlete.java` NO modificado, `SecurityConfig` sin `permitAll()` nuevos. Reporta hallazgos (no implementa fixes salvo instrucción explícita).
- [ ] 6.9 Actualizar `TASKS.md` raíz: marcar módulo Checkup completo; ajustar item `medal_projections` (fila `TASKS.md:78`) como deuda explícita con referencia a D5 del design; registrar al final la nota de decisión D5. NO crear CHANGELOG.md (política de repo). Confirmar con usuario antes de cerrar el módulo.
